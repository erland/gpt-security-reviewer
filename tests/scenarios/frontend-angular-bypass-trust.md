# Scenario: Angular bypassSecurityTrustHtml

## Input
API-data skickas till `bypassSecurityTrustHtml()` och resultatet används i `[innerHTML]`.

## Expected
Högprioriterad granskningspunkt eller fynd beroende på datakälla och separat sanering.
