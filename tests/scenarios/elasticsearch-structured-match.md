# Scenario: Elasticsearch strukturerad match-query

## Input
Användarens söktext sätts som värde i en fast `match`-query; index och fält är serverstyrda.

## Expected
Rapportera inte query injection enbart på denna grund.
